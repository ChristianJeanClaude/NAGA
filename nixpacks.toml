[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "chromium", "playwright-driver"]

[phases.install]
cmds = [
    "pip install -r requirements.txt --break-system-packages",
    "python -m playwright install chromium"
]

[start]
cmd = "python main.py"
